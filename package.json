{
  "name": "tailwind-prac",
  "version": "1.0.0",
  "description": "Web development portfolio with TypeScript",
  "scripts": {
    "build": "tsc && echo 'Build complete'",
    "dev": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
